# Python packages for the Ubuntu 24.04.4 Ansible bastion.
# Install these inside the repo venv:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   python -m pip install -r requirements-python.txt

ansible-core>=2.17,<2.20
pyvmomi>=8.0.0
requests>=2.31.0
PyYAML>=6.0.1
jmespath>=1.0.1
netaddr>=1.2.1
pywinrm>=0.5.0
dnspython>=2.6.1
kubernetes>=29.0.0
jsonpatch>=1.33
